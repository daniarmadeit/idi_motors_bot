# 🚗 BeForward Parser Bot

Telegram бот для парсинга данных автомобилей с BeForward.jp с автоматической очисткой водяных знаков с фотографий.

## ✨ Возможности

- 📊 Парсинг характеристик автомобилей с BeForward.jp
- 💰 Автоматическое получение цены доставки (Dar es Salaam, RORO)
- 📷 Скачивание до 20 фотографий за раз
- ✨ **Автоматическое удаление водяных знаков** с помощью IOPaint AI
- 🔍 **Умный AI Upscaling** - автоматическое улучшение качества фото с низким разрешением
- 🤖 **Генерация продающих описаний** через OpenAI GPT-5-nano
- 📤 Отправка фото альбомом в Telegram + ZIP архив
- 📥 Экспорт данных в TXT файл
- 🌍 Поддержка двух версий сайта BeForward

## 🔧 Установка

### Быстрая установка (Windows)

1. Установите Python 3.8+
2. Скачайте проект
3. Запустите `install.bat`
4. Готово! ✅

### Ручная установка

### 1. Установите Python 3.8+

### 2. Клонируйте репозиторий

```bash
git clone <repository-url>
cd rus_bot
```

### 3. Создайте виртуальное окружение

**Windows:**
```bash
python -m venv venv
venv\Scripts\activate
```

**Linux/macOS:**
```bash
python3 -m venv venv
source venv/bin/activate
```

### 4. Установите зависимости

```bash
pip install -r requirements.txt
```

### 5. Настройте токены

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и добавьте ваши токены:

```
BOT_TOKEN=your_telegram_bot_token_here
OPENAI_API_KEY=your_openai_api_key_here
```

**Важно:** Не коммитьте `.env` файл в Git!

## 🚀 Запуск

### ⚠️ ВАЖНО: Нужно запустить 2 процесса!

Бот работает в связке с IOPaint сервером. Нужны **два отдельных окна**:

### Окно 1: IOPaint Сервер

**Windows:**
```bash
start_iopaint.bat
```

**Ручной запуск:**
```bash
venv\Scripts\activate
iopaint start --model=lama --device=cpu --port=8085 --host=127.0.0.1
```

При первом запуске IOPaint автоматически скачает модель LaMa (~200MB).

### Окно 2: Telegram Bot

**Windows:**
```bash
run.bat
```

**Ручной запуск:**
```bash
venv\Scripts\activate
python rus_bot.py
```

### 📝 Порядок запуска:

1. **Сначала** запустите IOPaint сервер (`start_iopaint.bat`)
2. Дождитесь сообщения "Running on http://127.0.0.1:8085"
3. **Затем** запустите бота (`run.bat`)
4. Не закрывайте оба окна во время работы!

## 📱 Использование

1. Запустите бота командой `/start`
2. Отправьте боту ссылку на BeForward.jp
3. Получите данные автомобиля с кнопками:
   - **"✨ Генерировать продающее описание"** - создаёт профессиональное описание через GPT-5-nano
   - **"📥 Скачать TXT"** - экспорт характеристик в текстовый файл
   - **"📷 Скачать все фото (очищенные)"** - фото без водяных знаков + AI upscaling

## 🎨 IOPaint - Удаление водяных знаков + AI Upscaling

Бот использует [IOPaint](https://github.com/Sanster/IOPaint) с несколькими AI моделями:

- 🤖 **LaMa** - удаление водяных знаков (State-of-the-art inpainting)
- 🔍 **RealESRGAN** - улучшение качества фото низкого разрешения
- 📍 **Позиция водяного знака**: 300×30px, по центру снизу
- 🌐 **Архитектура**: HTTP API сервер (порт 8085)
- ⚡ **Обработка**: CPU (можно переключить на GPU)
- 📦 **Лимит**: до 20 фото за раз

### Как это работает:

1. IOPaint запускается как отдельный HTTP сервер
2. Бот скачивает ZIP с фотографиями (до 20 штук)
3. **Умная обработка каждого фото:**
   - **ШАГ 1:** 🎭 Создание маски водяного знака (300×30px снизу по центру)
   - **ШАГ 2:** ✨ Удаление водяного знака через LaMa (на оригинальном разрешении)
   - **ШАГ 3:** 🔍 Проверка разрешения (минимум: 1920×1080 Full HD)
   - **ШАГ 4:** 📈 Если разрешение ниже → автоматический AI upscaling через RealESRGAN (2x)
   - **ШАГ 5:** ✅ Если разрешение норм → пропускаем upscaling
4. Очищенные фото отправляются альбомом в Telegram (до 10 фото в альбоме)
5. ZIP архив доступен для скачивания по кнопке

**Почему именно в таком порядке?**
Сначала удаляем водяной знак на оригинальном разрешении - так AI лучше понимает контекст и результат качественнее. Потом уже апскейлим чистое изображение.

## 🤖 OpenAI GPT-5-nano - Генерация продающих описаний

Бот использует **GPT-5-nano** от OpenAI для создания профессиональных продающих описаний:

- 📝 **Автоматический копирайтинг** на основе характеристик автомобиля
- 🎯 **Фокус на африканский рынок** - учитывает специфику покупателей
- ✨ **Профессиональный стиль** - подчёркивает преимущества и ценность
- ⚡ **Быстро** - генерация за 2-5 секунд
- 💰 **Экономично** - GPT-5-nano оптимизирована для скорости и низкой стоимости

### Что включает описание:

- Ключевые характеристики и преимущества
- Акцент на надёжность и качество
- Призыв к действию для покупателей
- 150-200 слов профессионального текста

### Пример использования:

1. Отправьте ссылку на BeForward.jp
2. Нажмите **"✨ Генерировать продающее описание"**
3. Получите готовый текст для объявления на английском

**API Key:** Настраивается в `.env` файле как `OPENAI_API_KEY`

## ⚙️ Настройка

### Использование GPU

Для значительного ускорения обработки (особенно upscaling) запустите IOPaint с параметром `--device=cuda`:

**В start_iopaint.bat замените:**
```bash
--device=cpu
```

**На:**
```bash
--device=cuda
```

**Полная команда с GPU:**
```bash
iopaint start --model=lama --device=cuda --port=8085 --host=127.0.0.1 --enable-realesrgan --realesrgan-model=realesr-general-x4v3
```

⚡ **Рекомендуется GPU** для обработки 20 фото - будет в 5-10 раз быстрее!

### Настройка минимального разрешения для upscaling

В файле `rus_bot.py`, метод `download_and_process_photos` (строка ~358):

```python
MIN_RESOLUTION = 1920 * 1080  # Минимальное разрешение (Full HD)
```

Фото с разрешением ниже этого значения будут автоматически улучшены через AI upscaling (2x).

### Изменение размера маски водяного знака

В файле `rus_bot.py`, метод `download_and_process_photos` (строки ~419-420):

```python
watermark_width = 300  # Ширина маски
watermark_height = 30  # Высота маски
```

### Изменение лимита фото

В файле `rus_bot.py`, метод `download_and_process_photos` (строка ~347):

```python
image_files_limited = image_files[:20]  # Продакшен лимит
```

## 📋 Парсинг данных

Бот извлекает:

- ✅ Название автомобиля
- ✅ Цену доставки (Dar es Salaam, RORO)
- ✅ Пробег (Mileage)
- ✅ Объем двигателя (Engine Size)
- ✅ Цвет кузова (Ext. Color)
- ✅ Топливо (Fuel)
- ✅ Год регистрации
- ✅ И многое другое...

**Исключаются**: Chassis No, Sub Ref No, Ref. No

**Фильтрация**: Характеристики со значением "-" не выводятся

## 🔐 Безопасность

⚠️ **Важно**: Не храните токен бота в коде! Используйте `.env` файл или переменные окружения.

## 📁 Структура проекта

```
rus_bot/
├── rus_bot.py          # Основной файл бота
├── config.py           # Конфигурация и константы
├── requirements.txt    # Python зависимости
├── .env               # Переменные окружения (создаётся вручную)
├── .env.example       # Пример конфигурации
├── .gitignore         # Игнорируемые файлы
├── install.bat        # Скрипт установки (Windows)
├── run.bat            # Запуск бота (Windows)
└── start_iopaint.bat  # Запуск IOPaint сервера (Windows)
```

## 📦 Зависимости

- `python-telegram-bot` - Telegram Bot API
- `beautifulsoup4` - Парсинг HTML
- `requests` - HTTP запросы
- `Pillow` - Обработка изображений
- `iopaint` - AI удаление водяных знаков + upscaling
- `openai` - Генерация продающих описаний через GPT-5-nano
- `python-dotenv` - Переменные окружения

## 🐛 Отладка

Логи сохраняются в консоль с подробной информацией:

```
🌍 URL с параметром страны
🔍 Найдено городов
✅ Берём первый город
💰 Найдена цена
📥 Скачиваем фото
🎨 Запускаем IOPaint
```

## 📄 Лицензия

Apache-2.0 (для IOPaint)

## 🙏 Благодарности

- [IOPaint](https://github.com/Sanster/IOPaint) - За отличный инструмент AI inpainting
- [python-telegram-bot](https://github.com/python-telegram-bot/python-telegram-bot) - За удобную библиотеку

## 🔗 Ссылки

- IOPaint GitHub: https://github.com/Sanster/IOPaint
- BeForward.jp: https://www.beforward.jp

